# Studies

Findings worth keeping: measured, reproducible, and load-bearing for a decision in this repo.
Unlike [`../experiments/`](../experiments/README.md), everything here is meant to survive.

| File | Finding |
|---|---|
| `rendering-cost.md` | GPU scene-graph toolkits cost real, permanent VRAM for work CPU-side toolkits do for free — the constraint every default in `profiles/desktop.nix` follows from. |
